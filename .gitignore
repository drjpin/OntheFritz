# PHP config (contains secrets — never commit)
config.php
hub/config.php
backups/
usage.json
login_attempts.json

# Dependencies
/node_modules
/.pnp
.pnp.*

# Next.js (old stack — no longer used)
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files
.env*

# Vercel / Cloudflare (old stack)
.vercel
wrangler-dist/
